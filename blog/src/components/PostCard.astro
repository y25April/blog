---
const { post } = Astro.props;
---

<a href={`/blog/${post.slug}/`} class="block p-6 rounded-2xl shadow hover:shadow-lg transition bg-base-200">
  <h2 class="text-2xl font-semibold">{post.data.title}</h2>
  <p class="text-sm opacity-70 mt-1">{post.data.date.toLocaleDateString("ja-JP")}</p>
  <p class="mt-3">{post.data.description}</p>
</a>